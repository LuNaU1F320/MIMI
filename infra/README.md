# Infrastructure

Infrastructure, deployment, and operations assets belong here.

Examples:

- Local docker-compose files
- Cloud deployment definitions
- Environment templates
- Observability configuration

The initial implementation should remain local-first. Cloud infrastructure should be added after the backend session model is stable.

